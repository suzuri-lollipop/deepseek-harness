org.gradle.jvmargs=-Xmx3072m -Dfile.encoding=UTF-8
android.useAndroidX=true
org.gradle.configuration-cache=false
# Compile inside the Gradle daemon: the Kotlin daemon's default base dir
# (user home) is not writable in the agent sandbox.
kotlin.compiler.execution.strategy=in-process
# Offline builds: the Maven aapt2 artifact for AGP 8.13.2 is not in the
# offline cache, so use the aapt2 binary from build-tools 36.0.0.
android.aapt2FromMavenOverride=C:/Users/Suzuri/AppData/Local/Android/Sdk/build-tools/36.0.0/aapt2.exe
